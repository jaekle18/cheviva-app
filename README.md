# Cheviva

The Cheviva app's own page, for the phones' home screen. Built from `src/app.html` of the private Cheviva system by
`tools/construir-app-web.py`. It holds no business data: every number comes from the sheet, and only with the code each
phone types once.
